/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ['class', 'dark-theme'],
  content: ['./src/**/*.{html,ts}'],
  theme: {
    /* extend: {
      colors: {
        basebg: 'var(--bg)',
        basetext: 'var(--text)',
      },
    }, */
  },
  plugins: [],
};
